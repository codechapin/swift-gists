Writing raw pixels to an image. Using raw pointers to learn how to use them.

Easy mode to run the code:
```bash
swift main.swift
```

Compile and run:
```bash
make build
make run
```

Clean
```bash
make clean
```